{
  "name": "sapper",
  "version": "0.1.0",
  "description": "",
  "main": "public/main.mjs",
  "scripts": {
    "watch": "pnpm exec squint watch",
    "serve": "pnpm live-server --port=8000 --no-browser public/",
    "dev": "concurrently \"pnpm watch\" \"pnpm serve\"",
    "build:wasm": "zig build-exe src-zig/solver.zig -target wasm32-freestanding -fno-entry -O ReleaseFast -femit-bin=public/solver.wasm --export=solve --export=hint --export=getInputBuf --export=getOutputBuf"
  },
  "keywords": [],
  "author": "Nikita Prokopov",
  "license": "MIT",
  "packageManager": "pnpm@10.28.1",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "live-server": "^1.2.2",
    "speedscope": "^1.25.0"
  },
  "dependencies": {
    "squint-cljs": "link:/Users/tonsky/ws/squint"
  }
}
